## Outputs

```yaml
Outputs:
  SecurityGroup1:
    Description:
    Value: !Ref MyCompanySecurityGroup   # this must be defined in Resource section
    Export:
      Name: MySecurityGroup
      # Other stacks can use `SecurityGroupId: !ImportValue MySecurityGroup`
```
